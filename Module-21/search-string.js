const string = 'Nothing to hide, your are on the serveillance eye'
const serachString = 'noThing'
const doesExist = string.toLowerCase().includes(serachString.toLowerCase());
// console.log(doesExist);
const idx = string.indexOf('hide');
console.log(idx);